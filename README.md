# Journey Package Library

Static catalog and package hosting for the Journey Android application.

- `catalog.json` contains package summaries used by the Journey Library.
- `packages/` contains complete Journey package JSON files.

# Example Commands:
-  git add source/packages/great_american_lakes.json
-  git commit -m "Add Great American Lakes Journey"
-  git push
-  Create Pull Request